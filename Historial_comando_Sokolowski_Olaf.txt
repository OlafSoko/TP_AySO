ssh-keygen
cat $HOME/.ssh/*.pub
cd
mkdir repogit
ls
cd repogit
git clone git@github.com:OlafSoko/TP_AySO.git
cd TP_AySO/
pwd
ls -l
cat /etc/passwd
man grep
grep UBUNTU /etc/passwd 
grep ubuntu /etc/passwd
grep -i Ubuntu /etc/passwd
cat /etc/shadow
sudo cat /etc/shadow
grep -i Ubuntu /etc/shadow
sudo grep -i Ubuntu /etc/shadow
sudo grep -i Ubuntu /etc/shadow | awk -F ':'
sudo grep -i Ubuntu /etc/shadow | awk -F ':' '{print$2}'
cd
cd repogit/TP_AySO/
echo "!" > datos_usuario.txt 
ls
whoami
echo “Usuario=$(whoami)” >> datos_usuario.txt
git add .
git config --global user.email "olaf.soko@gmail.com"
git config --global user.name "OlafSoko"
git commit -m "ADD: agregado 1er ejercicio
sobre datos_usuarios.txt"
git status
echo "info del modelo cpu" > info_pc.txt
ls
grep "model name" /proc/cpuinfo
grep "model name" /proc/cpuinfo | tail -n1 >> info_pc.txt
cat info_pc.txt
git add .
git commit -m “feat: Añadiendo información de CPU”
git commit -m "feat: Añadiendo informacion de CPU"
git status
echo "" > lista_ordenada
ls
man grep
man sort
grep -v "nologin" /etc/passwd | head -n 10 | sort -t: -k7  > lista_ordenada
cat lista_ordenada
git add .
git config --global user.email "olaf.soko@gmail.com"
git config --global user.name "OlafSoko"
git commit -m “ADD: Listado de usuarios ordenada”
git commit -m "ADD: Listado de usuarios ordenada"
git status
ls
cat README.md
cat << EOF >> README.md
Alumno: Olaf Sokolowski
Division: 115
Turno: mañana
EOF

git add.
git add .
git config --global user.email "olaf.soko@gmail.com"
git config --global user.name "OlafSoko"
git commit -m "feat: añadiendo informacion del alumno al README"
git status
tree
sudo apt install tree
tree
mkdir -p TP1/{arquitectura}/clase{1..5}, /{so}/clase{1..6}
sudo mkdir -p TP1/{arquitectura}/clase{1..5}, /{so}/clase{1..6}
tree
rm -rf TP1
LS
ls
cd
sudo mkdir -p TP1/{arquitectura}/clase{1..5} , {so}/clase{1..6}
tree
cat  << EOF >  script_dir.sh
#!/bin/bash
sudo mkdir -p TP1/{arquitectura}/clase{1..5} , {so}/clase{1..6}

EOF

chmod man
chmod + script_dir.sh
git add .
cd ~
rm -rf TP1/ \{so\}/ , script_dir.sh
sudo rm -rf TP1/ \{so\}/ , script_dir.sh
tree
cd /repogit
cd /repogit/TP_AySO
cd ~/repogit/TP_AySO
tree
mkdir -p TP1/arquitectura/clase{1..5} TP1/so/clase{1..6}
tree
cat << EOF > script_dir.sh
#!/bin/bash
mkdir -p /TP1/arquitectura/clase{1..5} /TP1/so/clase{1..6}
EOF

git add .
git commit -m "ADD: Añadiendo script"
git status
git push
history
history -a
